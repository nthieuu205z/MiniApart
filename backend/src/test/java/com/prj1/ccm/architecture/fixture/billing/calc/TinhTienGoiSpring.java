package com.prj1.ccm.architecture.fixture.billing.calc;

import org.springframework.stereotype.Component;

/**
 * Deliberately violates convention 2 by dragging Spring into a calculation class, so
 * {@code ArchitectureRulesBiteTest} can prove the rule catches it. A specimen, not an example.
 */
@Component
public class TinhTienGoiSpring {
}
