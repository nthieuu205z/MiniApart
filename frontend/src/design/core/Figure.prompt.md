Con số. Dùng cho mọi giá trị đọc bằng mắt để so sánh.

```jsx
<Figure value="8.450.000" unit="đ" tone="urgent" />
<Figure value={21} total={24} />
<Figure value="58" unit="kWh" size="sm" />
```

Trong bảng, đặt trong ô `text-align: right` để số thẳng cột. Cụm tổng có gạch trên 2px màu mực.
